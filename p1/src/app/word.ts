export class Word {
    palavra: string
    significado: string

    constructor(palavra?: string, significado?: string) {
        this.palavra = palavra
        this.significado = significado
    }
}